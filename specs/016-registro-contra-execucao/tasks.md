# Tarefas — 016-registro-contra-execucao

> Fase 3 · dono: tech-lead · ids [TNNN] são permanentes; wave final é sempre validação.
> Consome o [plan.md](plan.md) (portão da Fase 2: *"Prossiga com as tarefas da 016"*,
> 2026-09-04) e a [spec.md](spec.md) com as erratas E1/E2 (portão da Fase 1). As
> decisões dos dois portões estão no planning-state (`specify`, `plan`) e **não se
> reabrem aqui**. O tech-lead **propõe**; quem delega é o orquestrador (R5).

| Id | Wave | Dono (agente) | Tipo | [P] | Descrição | Gate associado |
|---|---|---|---|---|---|---|
| T001 | 0 | build-engineer | chore | | **Repin R0b** — o R0 do plano (errata + `plan.md` + `tasks.md`) foi consumido em `9e3fb0c` **antes** deste arquivo existir; medido agora: `check_baseline` `393/393 · 0 sem pin`, logo o commit deste `tasks.md` deixa **1 rastreado sem pin**. Commit chore próprio, imediatamente depois do commit do `tasks.md`. Mensagem: `chore(016): gen_pins — R0b (tasks.md)`. Refinamento de granularidade da série do plano, não desvio | stage `baseline` |
| T002 | 0 | orquestrador (skill `new-demand`) | chore | [P] | Planning-state: registrar o portão da Fase 3 (aprovação **literal** do usuário no chat) e `phase: red`. `.claude/project-memory/**` é exclusão do `gen_pins.py:29` — **não pede repin** | stage `state` |
| T010 | 1 | qa-engineer | feature | [P] | `.claude/verify/fecho.json` (spec §Contratos: `piso` = `921977c25e76fe0ed19dae74e17921d37c711ff0`; `excluidas_por_r13` 003 (2 artefatos) · 009 · 010 com `fonte`; `populacao` com o padrão `^feature/(\d{3})-` e as três `integracoes_de_main`; `sonda.total: 26` + `casos[{id, esperado}]` F1–F19, P1–P7; `_meta.prova_de_carga` com o literal `"a medir na Fase 4/5"` nas três chaves `piso`/`exclusoes`/`fec1`) **+** `.claude/verify/fixtures_016/fecho/` — **26 arquivos, um por caso, só entradas** (`{id, modo, estados, merges[posicao_relativa_ao_piso], ancestralidade, artefatos, data_do_commit, registro, origin_develop, head_ref, base_ref}` — plan §Contratos); o `esperado` **nunca** fica na fixture. Nome de arquivo derivável do id (a guarda 2 do plano casa arquivo ↔ caso ↔ total). Red: T022 | `D016-FEC1`…`FEC4`, `D016-PR1`, C7 |
| T011 | 1 | qa-engineer | feature | [P] | `.claude/verify/branch_protection.json` (`repo`, `ref: develop`, `checks_obrigatorios: ["verify","visual","fecho"]`, `up_to_date: true`, `sonda.total: 9`, casos `hoje`, `esperado_ruleset`, `sem_visual`, `sem_fecho`, `strict_false`, `classic_off`, `classic_on`, `http_403`, `sem_rede` — E1) **+** `.claude/verify/fixtures_016/protecao/` — **9 arquivos**, cada um com as duas respostas de API como `{status, body, erro}`; `hoje` carrega as respostas **reais** de 2026-09-04 (`protected: true` + `protection.enabled: false`; ruleset 21381133 só `deletion`/`non_fast_forward`). Autor nesta wave: QA (valores são a decisão P2 transcrita); **owner do estado** daqui em diante: `build-engineer` (plan §Owner). Red: T022 | `D016-PROT1`, C7 |
| T012 | 1 | data-engineer | chore | [P] | `.claude/templates/planning-state.schema.json`: `required` passa a `["demanda","phase","spec_dir","branch"]` (hoje sem `branch`, medido); propriedade `fecho_pendente` `{motivo, dono, prazo, declarado_em?}` com `required: ["motivo","dono","prazo"]` e `pattern ^\d{4}-\d{2}-\d{2}$` em `prazo`/`declarado_em`. Tipado `chore` porque **nenhum executável lê o schema** (`check_state.py` não valida por biblioteca — spec §Superfície 4); o comportamento de `fecho_pendente` tem red em T022 (F6–F10) e é julgado pelo stage `fecho`. Chave irmã `validacao`/`implementacao` (010/011/015) **não** se toca (spec §Fora de escopo) | — (contrato) |
| T013 | 1 | build-engineer | chore | | **Repin R1a** — fecha o commit de T010+T011 (`feat(016): registros e fixtures da sonda D016`). Se T010 e T011 forem commitadas separadas, são **dois** repins (R1a/R1a′), nunca um | stage `baseline` |
| T014 | 1 | build-engineer | chore | | **Repin R1b** — fecha o commit de T012 (schema) | stage `baseline` |
| T020 | 2 | qa-engineer | feature | [P] | **RED** — `.claude/verify/check_fecho.py`, o **gate** (plan §Módulos): CLI `[--pr] [--sonda] [--json]` (ao vivo **não** aceita `--head`/`--base`); ordem fixa **sonda → leitura → julgamento → relato**; importa `VEREDITOS` e `julgar_pos_merge`/`julgar_pre_merge`/`ler_*` de `fecho` (**que ainda não existe**): instrumento ausente ⇒ cada caso sai `obtido: INSTRUMENTO AUSENTE` ≠ esperado ⇒ **26 divergências nomeadas**, exit 1; guarda 2 (arquivos na pasta == `len(casos)` == `total`, FAIL nomeando o id); `--sonda`/`--json` = **um JSON em stdout**, prosa em stderr; vocabulário T10; nenhum `if` que produza veredito (plan: FAIL de revisão); `sys.stdout.reconfigure(encoding="utf-8")`; `git` por lista de argumentos; nada escreve; **zero `urllib`/`http`/`socket`/`ssl`** | `D016-FEC1`…`FEC4`, `D016-PR1`, C7 |
| T021 | 2 | qa-engineer | feature | [P] | **RED** — `.claude/verify/check_branch_protection.py`, o **gate**: CLI `[--sonda] [--fixture <dir>] [--json]`; importa `classificar`/`ler_api` de `branch_protection` (ausente) ⇒ **9 divergências**, exit 1; política T7 (`severidade` no JSON: `PASS`/`FAIL`/`WARN`; `NÃO DETERMINÁVEL` ⇒ WARN local, FAIL sob `GITHUB_ACTIONS`; `strict` sob classic ⇒ WARN permanente); imprime a **fonte** do token, nunca o token; primeira linha declara que lê o mundo (R7) | `D016-PROT1`, C7 |
| T022 | 2 | qa-engineer | chore | | **Executar e COMMITAR o FAIL** (R3 §4): `python .claude/verify/check_fecho.py --sonda` (26 divergências), `check_branch_protection.py --sonda` (9), e `GITHUB_HEAD_REF=feature/016-registro-contra-execucao GITHUB_BASE_REF=develop python .claude/verify/check_fecho.py --pr` (FAIL). Saída integral em `specs/016-registro-contra-execucao/red-016.md`, no mesmo commit dos dois gates: `test(016): red — sondas D016 (26 + 9 divergências; instrumentos ausentes)`. Planning-state: `red.status: proven` + `red.commit` (SHA completo) + `phase: implement` (orquestrador, sem repin). **Nada entra no `pipeline.yaml`** — o red se prova executando o gate; o `--light` do hook Stop segue 11 PASS | stage `tdd` |
| T023 | 2 | build-engineer | chore | | **Repin R2** — fecha o commit red (gates + `red-016.md`) | stage `baseline` |
| T030 | 3 | core-engineer | feature | [P] | `.claude/verify/fecho.py`, o **instrumento**: `VEREDITOS` (10 valores de T10, fechado), `julgar_pos_merge(estados, merges, ancestralidade, artefatos, data_do_commit, registro, origin_develop)` e `julgar_pre_merge(head_ref, base_ref, estados, artefatos, registro)` **puros** (assinaturas e retornos no plan §Contratos — as fixtures de T010 já obedecem a elas); leitores `ler_estados`, `ler_merges(piso)` (*(ET2)* `git log --first-parent --format=%H%x00%P%x00%cI%x00%s refs/remotes/origin/develop` — a cadeia **inteira**, sem `--merges`: o piso pode ser commit não-merge; sujeito é só quem tem ≥ 2 pais), `ler_ancestralidade(estados, piso)` (*(ET1)* `rev-parse --verify <sha>^{commit}` antes de `merge-base --is-ancestor` contra `origin/develop` **e** contra o piso ⇒ `anterior_ao_piso`; ambíguo ⇒ `null` com causa), `ler_data_commit()` (`%cI` de HEAD, dia ISO), `ler_artefatos(estados)`; padrão de população compilado **uma vez** a partir de `registro.populacao`. Sem `print`, sem `sys.exit`, sem rede, nada escreve. Pronto quando `check_fecho.py --sonda` responde **26/26** (19 pós + 7 pré). **Não edita o gate**: mudança necessária volta por `DEPENDÊNCIAS` ao `qa-engineer` (R3 §2). Orçamento ~350 linhas; > 600 ⇒ `fecho_leitura.py`, motivo no commit | `D016-FEC1`…`FEC4`, `D016-PR1` (green) |
| T031 | 3 | build-engineer | feature | [P] | `.claude/verify/branch_protection.py`, o **instrumento**: `classificar(rules, branch, esperado, modo_ci) -> {veredito, faltam, mecanismo, contextos, strict, outras_regras, avisos, severidade}` **puro** (`PROTEGIDA` sse os três contextos por ≥ 1 mecanismo **e** `strict_required_status_checks_policy: true`; `protected: true` + `enabled: false`/`enforcement_level: "off"` + ruleset sem `required_status_checks` ⇒ `DESPROTEGIDA` — T6); `ler_api(repo, ref)` — dois GETs (`/rules/branches/{ref}`, `/branches/{ref}`), `urllib` com timeout, redirect sem token (forma de `check_evidence_bridge.py`), token `GITHUB_TOKEN` → `gh auth token` → nenhum, repositório `GITHUB_REPOSITORY` → `git remote get-url origin`. Pronto quando `check_branch_protection.py --sonda` responde **9/9**. Não edita o gate | `D016-PROT1` (green) |
| T032 | 3 | qa-engineer | chore | | **Medição da W3** (nada escrito): (i) `python .claude/verify/check_fecho.py` nu ⇒ `[SONDA] 26 · 0`, 10 `CONFORME` (3 com `EXCLUÍDA R13` impressa e fonte), `016: EM VOO`, `origin/develop julgado: 921977c2…`, `0 problema(s)`; (ii) `--pr` com `GITHUB_HEAD_REF=feature/016-…`/`GITHUB_BASE_REF=develop` ⇒ `FECHO PENDENTE da demanda 016 (fase implement) — merge bloqueado até done`, exit 1 — **o red de `D016-PR1` pela razão certa, local**; (iii) `check_branch_protection.py` com `gh auth token` ⇒ `DESPROTEGIDA · faltam: verify, visual, fecho, up-to-date`, exit 1 — **red de `D016-PROT1` pela razão certa, local**; sem token/rede ⇒ `[WARN] NÃO DETERMINÁVEL (…)`; (iv) `time` do gate nu (limiar do plan §Riscos: 2 s); (v) *(ET4)* `grep -nwE "urllib|http|socket|ssl" .claude/verify/check_fecho.py .claude/verify/fecho.py` ⇒ **zero** — palavra inteira, arquivo inteiro; a lista nominal vive **só aqui**, fora dos dois arquivos varridos (R10 §10). Saídas guardadas para `red-016.md`? **Não** — vão para `prova-de-carga.md` (T061) | `D016-PR1`, `D016-PROT1` (red vivo local) |
| T033 | 3 | build-engineer | chore | | **Repin R3a** — fecha o commit de `fecho.py` | stage `baseline` |
| T034 | 3 | build-engineer | chore | | **Repin R3b** — fecha o commit de `branch_protection.py` | stage `baseline` |
| T040 | 4 | build-engineer | chore | | **Cabeamento (a), um commit**: `.claude/verify/pipeline.yaml` — stage `fecho` **depois de `tdd`** (`:71-76`) e antes de `mutation`, `run: python .claude/verify/check_fecho.py`, `parallel: true`, `mutates: false`, `heavy: false`, `desc` do plan §Stage (nomeia a direção pré-merge no job) — o `--light` passa de **11 para 12** stages; `.github/workflows/verify.yml` — job **`fecho`** exatamente como no plan §Job (id sem `name:`, **sem `needs:`, sem `if:`**, `checkout@v7` raso, `setup-python@v7` 3.12, um passo `python .claude/verify/check_fecho.py --pr`, sem `permissions:`). `verify.yml:42` (`MUTATION_DEFER_MISSING`) e os jobs `verify`/`visual` **byte-idênticos** (T9). Mensagem: `chore(016): stage fecho no pipeline e job fecho no CI (T8-a, E1)` | `run.sh` · job `fecho` |
| T041 | 4 | build-engineer | chore | | **Repin R4a + push + abrir o PR** `feature/016-registro-contra-execucao → develop` (`gh pr create --base develop`; R14: abertura é livre). O PR abre **aqui**, não na Fase 6: `verify.yml:12-15` só dispara em `pull_request`/`push` para `develop`/`main` — push na feature não roda nada, e P2 precisa que o contexto `fecho` tenha reportado. Orquestrador grava `pr_url` no planning-state (sem repin). **Primeiro run** — registrar o número: job `fecho` **vermelho** com `FECHO PENDENTE da demanda 016 (fase implement)` = **red ao vivo de `D016-PR1`** (C5 b); job `verify` verde (stage `fecho`: 016 `EM VOO`; `origin/develop` presente com `fetch-depth: 0`); medir a **duração** do job `fecho` (estimativa do plano: dezenas de segundos — nunca prometida) | `D016-PR1` (red vivo, CI) |
| T042 | 4 | build-engineer | feature | | **Cabeamento (b) — por último na wave, porque liga o vermelho de `D016-PROT1` no `verify` do CI**: `.claude/verify/compliance-audit.sh` — `aviso()` com contador `WARN` ao lado de `ok()`/`falha()` (`:22-23`); seção **`branch-protection`** (cabeçalho `:7` lista a seção nova) que chama `python .claude/verify/check_branch_protection.py --json` e classifica **pela `severidade` do JSON**, nunca por regex sobre a prosa; `known-issues` (`:110-119`) ganha `fecho.json → excluidas_por_r13` como **terceira fonte** (estrutural: `fonte` obrigatória, sem prazo — classe `ESTRUTURAIS`); `waivers` (`:214-231`) lista `fecho_pendente` **pela chave JSON**, ao lado de `tdd_waiver`; linha final (`:311`) `compliance: N PASS · N FAIL · N WARN`, exit continua `$FAIL` — **antes**, `grep -rn "compliance:" .claude/ .github/` (a spec mediu: só o próprio script emite; reconferir). Red: T022 (9 divergências) + T043 (vivo). Mensagem: `feat(016): compliance-audit — seção branch-protection (D016-PROT1), aviso(), 3ª fonte, fecho_pendente` | `D016-PROT1` |
| T043 | 4 | build-engineer | chore | | **Repin R4b + push.** Segundo run — registrar o número: `verify` **vermelho** em `[FAIL] branch-protection: develop DESPROTEGIDA · faltam: verify, visual, fecho (checks obrigatórios), up-to-date` = **red ao vivo de `D016-PROT1`** (C6 c, o estado medido em 2026-09-04). **Medir a permissão do `GITHUB_TOKEN`** em `GET /rules/branches/develop` (a linha do gate imprime `token: GITHUB_TOKEN` e o status): 200 ⇒ segue; **403 ⇒ PARAR** e escalar ao proprietário com os dois lados do plano B (spec §Não mensurável 1: `permissions:` no job ou secret) — nenhum código muda sem decisão dele. Local: `compliance-audit.sh` com `gh auth token` fica vermelho na seção até P2 — esperado; o hook Stop **não** roda o audit (`post-turn-verify.sh:26` = `run.sh --light --no-mutate`) | `D016-PROT1` (red vivo, CI) |
| T050 | P2 | **usuário** | chore | | **Ato do proprietário, fora do repositório — depois do primeiro run de T041 (o contexto `fecho` só existe para o GitHub depois de reportar) e antes de qualquer tarefa da wave 7; não bloqueia as waves 5–6.** No GitHub, `oflavioc/quickscan-secops` → *Settings → Rules → Rulesets*: editar `MyRuleSet` (ruleset **21381133**, o que hoje tem `deletion` + `non_fast_forward` sobre `develop`) ou criar ruleset com target `develop`, e marcar: (1) **Require status checks to pass**; (2) adicionar os três contextos com estes nomes exatos — **`verify`**, **`visual`**, **`fecho`**; (3) **Require branches to be up to date before merging** (`strict_required_status_checks_policy: true`). Configurar **no ruleset, não na proteção clássica** (sob classic o `strict` não é legível pelo token — T7 e, `[WARN]` permanente). Recomendação da spec §NÃO mede 3, **fora de P2** e opcional: regra *Require a pull request* com *allowed merge methods = merge* (squash/rebase cegam os dois oráculos de T1). Confirmação **por execução**: o run seguinte (novo push ou re-run do `verify`) imprime `[PASS] branch-protection: develop PROTEGIDA · ruleset <id> · checks obrigatórios: verify, visual, fecho · up-to-date: sim`. Efeito imediato e **desejado**: o PR da 016 fica não mesclável até o `done` (check `fecho` vermelho). Se o ato atrasar, o vermelho tem dono e evento único (spec §Nascimento) — o PR não se mescla sem ele | `D016-PROT1` (verde vivo) |
| T060 | 5 | qa-engineer | feature | | `tests_016_mutants.js` **+** entrada `d016` em `.claude/verify/mutation_map.json` — **MESMO COMMIT** (D4 da 013: `check_mutation.py:283-298` reprova `preflight: true` sem `--preflight` em argv e derruba IC-4 e o stage; `check_suites.py:50-56` reprova `tests_*.js` fora do mapa). Entrada: `cmd: "node tests_016_mutants.js"`, `preflight: true`, `requires: ["node","python"]`, **`targets` explícitos, um a um** (`check_mutation.py:1287`: `t in changed`, pertinência exata num `set` — `fixtures_016/**` **não casa nada**): os 4 scripts, `fecho.json`, `branch_protection.json`, as **35 fixtures obtidas por `git ls-files .claude/verify/fixtures_016/`** (nunca digitadas) e o próprio harness — **42 entradas**. Forma de `tests_014_mutants.js`: mutação in-place + restauração por SHA-256 sob `try/finally` + `git status --porcelain` **escopado** (`:59-60`, `:100`, `:360-371`), `MUTATION_PY` (`:94-98`), `--preflight` → JSON C1 (`:337`), três estados de T4 da 013. **Mutantes de script** `D016-M1`…`M16` (spec §Critérios), cada um com `gate`, `find`/`repl` de ocorrência única e `reason` = **ids de caso** que têm de divergir no JSON de `--sonda` (`F19` para M1; `http_403` para M12; `total: 0` para M16…), nunca regex sobre prosa. **Mutantes de árvore** (ids **≥ `D016-M17`**, alocados aqui, sem reutilizar) — cada um é **o estado do mundo que faz o gate reprovar**, produzido por mutação e medido pelo **gate nu** (`check_fecho.py` sem flags), no molde do `D014-M4`: **(a)** `sem_fecho` — julgador que aceite dois dos três contextos (E1; fixture `sem_fecho` diverge); **(b) FEC1**: `fecho.json → piso.sha = 6dad53d3423b20c768690d0b0005ef88025b9f35` **e** `015-superficies-de-apoio.json → phase: "validate"` ⇒ FAIL nomeando **015 · MESCLADA SEM FECHO · oráculo: mensagem #34** e **nenhuma outra**; **(c) FEC2 / prova de carga do piso**: `piso.sha` = raiz `e5ccd429d0ed271ab3dd9ea948181e697f891af3` ("piso zero" — equivale a não ter piso sem cair em C2 d) ⇒ FAIL com **6 merges / 5 branches**: `#9` 000, `#10` e `#11` 001, `#12` 002, `#14` 004, `#15` 005 (medido por leitura da first-parent nesta Fase 3; `feat/*`, `fix/*` e as integrações de `main` saem `FORA DA POPULAÇÃO`); **(d) FEC3 / prova de carga das exclusões**: `excluidas_por_r13 = {}` ⇒ FAIL **003 (relatorio-final.md, spec-validate.md) · 009 (spec-validate.md) · 010 (spec-validate.md)** — 3 demandas / 4 artefatos; **(e) FEC4 / T5**: `fecho_pendente` **válido** escrito no planning-state da **016** (em voo) ⇒ FAIL `válvula antes do vencimento`; **(f) C7**: uma fixture removida ⇒ FAIL por contagem (arquivo ≠ caso ≠ total). Recomendados: **(g)** C1 f — `branch` removido de um estado `done` ⇒ FAIL de forma; **(h)** sobre o cenário (b), válvula em 015 com `prazo` `2026-01-01` ⇒ FAIL `vencida`, e com `prazo` ≥ `%cI` de HEAD ⇒ `FECHO PENDENTE DECLARADO`, exit 0 (o positivo prova que `ler_data_commit` e T4 funcionam na árvore). Campanha local sob árvore limpa: **todos KILL**; a primeira execução, com sobrevivente ou não, é registrada (R2 §1). Mensagem: `feat(016): harness d016 — mutantes de script M1–M16 e de árvore ≥ M17; entrada no mutation_map` | `IC-1`, `IC-4`, todos os `D016-*` |
| T061 | 5 | qa-engineer | chore | | **Um commit, três artefatos** (todos do QA): (1) `.claude/verify/mutation-matrix.json` — pares `D016-M1`…`M<n>` (`check_tdd.py:49-50`: `harness` + `gate` + `ultima_prova.resultado` em **todos**); desfecho anexado à dívida **"Borda 8"** como **sufixo da mesma string** (` · DESFECHO (016, 2026-09-04): credor = proteção de branch com `visual` obrigatório, auditada por D016-PROT1; a promessa continua, a cobrança existe` — `check_tdd.py:55-56` imprime `[DÍVIDA] {d}`, um objeto sairia como repr; texto original preservado, R2 §5); (2) `.claude/verify/fecho.json → _meta.prova_de_carga` **fixada pela saída da campanha de T060** — `piso`: "medido AAAA-MM-DD, D016-M<c>: 6 merges / 5 branches"; `exclusoes`: "… D016-M<d>: 003 (2), 009 (1), 010 (1)"; `fec1`: "… D016-M<b>: 015 por oráculo mensagem #34 com piso 6dad53d; restaurado ⇒ ANTERIOR AO PISO" — **lido ≠ medido é achado, não ajuste do número**; (3) `specs/016-registro-contra-execucao/prova-de-carga.md` — as saídas integrais de T032, dos mutantes de árvore e da **bateria adversarial em cópia efêmera** (`git worktree add <tmp>`, descartada): `git update-ref -d refs/remotes/origin/develop` ⇒ `[FAIL] NÃO DETERMINÁVEL (refs/remotes/origin/develop ausente — git fetch origin develop)`, exit 1, nunca SKIP; `GITHUB_TOKEN=invalido` ⇒ `NÃO DETERMINÁVEL (permissão 401)` → `[WARN]` local e `[FAIL]` com `GITHUB_ACTIONS=1`. Mensagem: `chore(016): matriz d016, prova de carga fixada por execução, bateria adversarial` | stage `tdd` · `D016-FEC1`…`FEC3` (carga) · C1 e · C6 d |
| T062 | 5 | build-engineer | chore | | **Repin R5a** — fecha T060 | stage `baseline` |
| T063 | 5 | build-engineer | chore | | **Repin R5b + push** — fecha T061. No run: a campanha `d016` roda **também no job `verify`** (node + python presentes; `MUTATION_DEFER_MISSING=1` não a defere) — conferir `d016` KILL n/n e **zero `[DEFER]`** (plan §Medição de campanhas) | stage `mutation` (CI) |
| T070 | 6 | doc-writer | doc | [P] | **C8, um commit**: `.claude/skills/new-demand/SKILL.md:66` (hoje *"Planning-state `done` só com PR aberto e CI verde"*) e `.claude/rules/sdd.md` §Gates de fase (item novo **Fecho**) com a redação da spec §Superfície 5 — ajuste de estilo permitido, **sentido não**: `done` = Fase 6 completa + PR aberto; CI verde é condição do **merge**, cobrada pela proteção (`D016-PROT1`); o merge espera o `done` (check `fecho`). Só na wave 6: o texto descreve o que os gates **já cobram** (C8). Mensagem: `doc(016): C8 — done sem CI verde; CI verde é condição do merge (P7)` | C8 (leitura no `spec-validate`) |
| T071 | 6 | doc-writer | doc | [P] | **C10-d + ADR, um commit**: `.claude/rules/design-decisions.md` — linha KI-3 (*"em calibração"* → calibrado na Onda 4; check obrigatório em `develop` desde a 016) e **linha confirmada nova** *"cobrança no merge vive na proteção de branch, auditada por `D016-PROT1`"* com a fonte (portão da Fase 0 da 016, 2026-09-04); `docs/adr/0001-cobranca-no-merge-fora-do-repositorio.md` pelo template `.claude/templates/adr.md`, com os três critérios da spec §Contratos (difícil de reverter · surpreendente sem contexto · trade-off 42–55 min/PR × configuração externa). ADR foi proposto na spec e no plano e o plano foi aprovado sem veto; **se o orquestrador o vetar, cai só o ADR** e nada mais muda | C10-d |
| T072 | 6 | product-owner | doc | [P] | `CONTEXT.md` §Estrutura (processo) — C9: os três verbetes de `refinement.md` §Vocabulário no formato R12 (**Termo** · definição · `_Evitar_`): *Fecho de demanda*, *Demanda mesclada sem fecho*, *Fecho pendente declarado* ("vencida" = prazo anterior à data do commit julgado — T4). *promessa de execução* e *deferimento vencido* **não entram** (P6) | C9 |
| T073 | 6 | build-engineer | chore | [P] | **Opcional (R-a6)** — `.claude/hooks/state-eval.sh:58-67`: onde hoje lista `phase != done` como "em curso", consultar `python .claude/verify/check_fecho.py --json` e imprimir por demanda `em voo` · `mesclada sem fecho` · `fecho pendente declarado`; qualquer erro do script ⇒ cai na listagem atual (o hook nunca bloqueia, nunca escreve). **Se não couber na wave, vira candidata no relatório final** (T082), sem gate — não é critério | — (informativo) |
| T074 | 6 | build-engineer | chore | | **Repin R6a** — fecha T070 | stage `baseline` |
| T075 | 6 | build-engineer | chore | | **Repin R6b** — fecha T071 | stage `baseline` |
| T076 | 6 | build-engineer | chore | | **Repin R6c** — fecha T072 | stage `baseline` |
| T077 | 6 | build-engineer | chore | | **Repin R6d + push** — fecha T073 (se houver; senão o push vai com R6c) | stage `baseline` |
| T080 | 7 | qa-engineer | chore | | **Validação executável** (planning-state `phase: validate` antes, pelo orquestrador). **Pré-condição: T050 feita** — `bash .claude/verify/compliance-audit.sh --rule=branch-protection` com `gh auth token` ⇒ `PROTEGIDA`; se não, **PARAR** (a wave 7 depende de P2). Depois: `bash .claude/verify/run.sh` completo (contagens **citadas**; `--light` = **12 PASS**); campanha `d016` KILL n/n **re-executada** (os mutantes de árvore b/c/d são a prova de carga rodando de novo — números iguais aos de `_meta.prova_de_carga`); **regressão nomeada**: `git diff --stat 921977c..HEAD` sem `check_state.py`, `check_tdd.py`, `check_mutation.py`, `env_doctor.py`, `expected_suites.json`, `invariants.json`, `known_issues.json`, `current_phase.json`, spec 013, e **nenhum path de produto**; `verify.yml:42` intacto; `grep` de rede em `check_fecho.py`/`fecho.py` ⇒ zero; `bridges.json` intacto. Então skill `spec-validate` contra a spec **com E1/E2** → `specs/016-registro-contra-execucao/spec-validate.md` (C8/C9/C10 por leitura; C1–C7 por execução citada) | pipeline completo · `d016` · `spec-validate` |
| T081 | 7 | product-owner | doc | [P] | **Aceite de intenção (Fase 6)** contra o `refinement.md` (P16: *o merge é o vencimento de toda promessa feita à verificação*): reprova ou declara *"não encontrei objeção"* — nunca escreve em registro de aceitação (D3). Conferir os **três flips medidos, não raciocinados**: check `fecho` do PR vermelho (T041) → verde só depois do `done` (T084); `branch-protection` vermelho (T043) → verde no run pós-P2; prova de carga com os números **da campanha** (T061). Conferir C8/C9 na letra | portão da Fase 6 |
| T082 | 7 | doc-writer | doc | [P] | `specs/016-registro-contra-execucao/relatorio-final.md` (PT-BR, R12; molde do da 015) — runs **citados por número**: o primeiro com `fecho` vermelho e sua duração (T041), o em que `D016-PROT1` ficou verde com o `ruleset_id` lido (pós-T050), a medição do token (T043), a campanha `d016` no CI (T063); **série de repins executada × prevista** (R0 consumido antes do `tasks.md` → R0b; R1a′ se houve; extras — ex. merge de `develop` exigido pelo *up to date* — nomeados, nunca silenciados); o não mensurável antes do merge, declarado: `origin/develop` no job `verify` em evento `push` pós-merge (remédio pronto: `git fetch origin develop`, plan §Riscos); candidatas com cadeia: P8 (`state-eval.sh:58-67`), chave irmã `validacao`/`implementacao` (`data-engineer`), R-a6 se T073 não coube. **+** `.claude/BACKLOG.md`: `EA-33` → `**Status**: \`resolvido\`` (forma canônica `compliance-audit.sh:257`; rodar `--rule=backlog` **antes** de commitar), `EA-14` **permanece `aberto`** com nota (C10-c) | R12 · `--rule=backlog` |
| T083 | 7 | build-engineer | chore | | **Fechamento + Repin R7 + push**: um commit de conteúdo com `spec-validate.md` + `relatorio-final.md` + `BACKLOG.md` (`doc(016): fechamento — spec-validate, relatório final, EA-33 resolvido`), repin em chore próprio. Asserção: `baseline` **0 divergentes · 0 sem pin** | stage `baseline` |
| T084 | 7 | orquestrador (skill `new-demand`) | chore | | Planning-state → **`done`** (P7: Fase 6 completa — `spec-validate.md` e `relatorio-final.md` em disco, `pr_url` presente — **sem exigir CI verde**); commit sem repin; push. O check **`fecho` reexecuta** ⇒ `LIBERADO · feature/016-registro-contra-execucao → develop · 016 em done · artefatos presentes`; `verify` e `visual` verdes. **Merge é do usuário**, no GitHub — o primeiro merge que a proteção P2 cobra | `D016-PR1` (verde vivo) |
| T085 | 7 | build-engineer | chore | [P] | **Medição, nada escrito** (depois de T050): `gh workflow run verify.yml --ref feature/016-registro-contra-execucao -f visual=false` ⇒ job `fecho` verde com `NÃO JULGADO (evento sem base — workflow_dispatch)` — **C5 g ao vivo**. Observar e **registrar, não julgar** (para T082): como o GitHub apresenta no PR o `visual` `skipped` desse run frente ao check obrigatório do mesmo head SHA — a spec (§bordas novas, 1ª linha) afirma que check não reportado bloqueia; a semântica de `skipped` em required check **não foi medida** em fase alguma | C5 g |

Tipos (R3): `feature`/`fix` exigem red provado; `refactor`/`doc`/`chore` não.
Um módulo por delegação — dois donos nunca no mesmo arquivo na mesma wave.
`[P]` = paralelizável dentro da wave (delegações na mesma mensagem).

## O estado do mundo que reprova cada gate — produzido e medido, nunca raciocinado

Exigência do portão: a 014 aposentou um problema alegando que um par media a
propriedade, e o CI provou que não media. Aqui **nenhum gate ficou sem estado
produzível**; a tabela diz qual é, quem o produz e quem o mede.

| gate | estado que o reprova | produzido em | medido em |
|---|---|---|---|
| `D016-FEC1` | demanda mesclada por PR após o piso com `phase != done`, sem válvula | T060 (b): piso `6dad53d` + 015 em `validate` (árvore real, restaurada por SHA); sonda F1/F2/F19 | T060, T061 (`_meta.prova_de_carga.fec1`), T080 (re-execução) |
| `D016-FEC2` | merge `feature/NNN-*` após o piso sem planning-state; merge fora de PR | T060 (c): piso zero ⇒ 6 merges / 5 branches de Onda; sonda F11/F13 | T060, T061, T080 |
| `D016-FEC3` | `done` sem artefato e sem exclusão; exclusão obsoleta | T060 (d): exclusões retiradas ⇒ 003 (2) · 009 · 010; sonda F15/F17 | T060, T061, T080 |
| `D016-FEC4` | válvula prematura / obsoleta / vencida / malformada | T060 (e): válvula na 016 em voo ⇒ `antes do vencimento`; (h) vencida e válida sobre 015 em validate; sonda F6–F10 (a árvore de hoje não tem válvula — **vácuo declarado**, spec §Guarda) | T060, T080 |
| `D016-PR1` | PR `feature/NNN` → `develop` com `phase != done` | **é o próprio PR da 016** até T084; local em T032 (ii) com `GITHUB_HEAD_REF` | T041 (run vermelho), T084 (flip para `LIBERADO`), T085 (`NÃO JULGADO` em dispatch) |
| `D016-PROT1` | `develop` sem os três contextos ou sem `strict` | **é a `develop` de hoje** (medida 2026-09-04) até T050; fixtures `hoje`/`sem_fecho`/`strict_false`/… | T032 (iii), T043 (run vermelho), pós-T050 (flip para `PROTEGIDA`), T080 |
| C7 (sonda) | sonda muda ou censo ≠ pinado | `D016-M16` (laço vazio) e T060 (f) (fixture removida) | T060, T080 |
| C1 e · C6 d | `origin/develop` ausente; token inválido / sem rede | bateria adversarial em cópia efêmera (T061) — I/O, não sonda | T061 (`prova-de-carga.md`) |
| C8 · C9 · C10 | — (prosa; sem gate por decisão da spec) | — | leitura no `spec-validate` (T080) e aceite (T081) |

## Notas de execução que o orquestrador precisa

- **Repin é sempre commit próprio e posterior** — `gen_pins.py` pina blobs de
  `HEAD`. **Um repin por commit de conteúdo, sem exceção**: previstos **R0b, R1a,
  R1b, R2, R3a, R3b, R4a, R4b, R5a, R5b, R6a, R6b, R6c, (R6d), R7 = 14–15**, além
  do R0 já executado. A 008 previu um e precisou de três; a 014, dezesseis. Repin
  fora desta lista (ex.: merge de `develop` na feature exigido pelo *up to date*
  de P2) vai **nomeado no relatório final** (T082), nunca silenciado.
- **T060 é indivisível**: harness e entrada `d016` no `mutation_map.json` no mesmo
  commit (D4 da 013; `check_suites.py:50-56`). Os `targets` saem de `git ls-files`,
  um a um — 42 entradas; glob não casa (`check_mutation.py:1287`).
- **T050 (P2) não é `[P]` com nada** e é a única tarefa com dono `usuário`. Depende
  do primeiro run de T041; a wave 7 inteira depende dela (T080 pára sem ela);
  as waves 5–6 **não** esperam por ela.
- **Um arquivo, um dono por wave**: `mutation_map.json`, `fecho.json`,
  `fixtures_016/**` e a matriz são do `qa-engineer` e nunca entram em wave de
  outro; `pipeline.yaml`, `verify.yml`, `compliance-audit.sh` são do
  `build-engineer` e ficam fora das waves 1–3; `branch_protection.json` tem autor
  QA na wave 1 e owner `build-engineer` depois (não muda nesta demanda);
  `check_fecho.py`/`check_branch_protection.py` são **só** do QA — o implementador
  que precisar de mudança no gate volta por `DEPENDÊNCIAS`.
- **O red de wave 2 não abre janela vermelha**: os gates só entram no
  `pipeline.yaml` na wave 4, já verdes; o `--light` do hook Stop vai de 11 para 12
  PASS em T040. O vermelho de `D016-PROT1` vive só no `compliance-audit` (CI e
  rito manual), não no hook.
- **Tipagem**: `feature` em T010/T011/T020/T021/T030/T031/T042/T060 — todos com
  red em T022 (26 + 9 divergências commitadas) e, para T042, também o vermelho
  vivo de T043; T012 é `chore` porque nenhum executável lê o schema (motivo na
  própria linha). Nenhuma tarefa `fix`.

## O gate que viaja no prompt (R3 §3)

Todo `D016-*` está definido em `specs/016-registro-contra-execucao/spec.md`
§*Critérios de aceite → gates* (com o mutante previsto) e as assinaturas em
`plan.md` §*Contratos e registros*. O prompt de cada delegação leva o **id** e
**esses caminhos**, nunca a asserção transcrita:

| Tarefa | Gate no prompt | Onde ler |
|---|---|---|
| T010 · T020 · T030 | `D016-FEC1`…`FEC4`, `D016-PR1`, C7 | spec §Critérios · §Contratos (`fecho.json`, casos F1–F19/P1–P7) · plan §Contratos (assinaturas) |
| T011 · T021 · T031 · T042 | `D016-PROT1`, C7 | spec §Critérios C6 · §Contratos (`branch_protection.json`, 9 casos) · T6/T7 |
| T040 · T041 | stage `fecho` · job `fecho` | plan §Stage · §Job (as cinco decisões) |
| T060 · T061 | `D016-M1`…`M16` + árvore ≥ `M17` | spec §Critérios (coluna mutante) · plan §Sonda pinada, guarda 3 |
| T070 · T071 · T072 | C8 · C10-d · C9 | spec §Superfície 5 · §Contratos (ADR) · `refinement.md` §Vocabulário |

## Errata — o que a execução mudou neste artefato (2026-09-04, wave 3)

Escrita pelo `qa-engineer` sob delegação do orquestrador, na forma da 015
(`specs/015-superficies-de-apoio/tasks.md` §Errata): id de tarefa é permanente,
nenhuma tarefa renumerada ou removida; as células de T030 e T032 foram
**amendadas com a marca** `*(ETn)*` e o texto original fica citado abaixo
(R2 §5). Precedência que o orquestrador fixou para a wave 3 e que estas
erratas só registram: **onde `tasks.md`/`plan.md` divergem do docstring de
`check_fecho.py` §CONTRATO DO INSTRUMENTO, vale o docstring** — é o texto mais
específico, foi escrito por quem cobra (R3 §3: é o gate que viaja no prompt) e
é o que a sonda pinada executa. O `spec-validate` da Fase 6 extrai critérios da
`spec.md` (`.claude/skills/spec-validate/SKILL.md:12`), não daqui; estas
erratas existem para o leitor humano não herdar assinatura errada.

| Id | O que este artefato dizia | O que vale (texto vigente) | Razão medida |
|---|---|---|---|
| **ET1** | T030: `ler_ancestralidade(estados)` — um parâmetro (idem `plan.md` §Contratos, "Leitores") | `ler_ancestralidade(estados, piso) -> {slug: {resposta, causa, anterior_ao_piso}}` — docstring de `check_fecho.py` (§CONTRATO, leitores); `red-016.md` §7; `fecho.py:516` | O julgador consome `anterior_ao_piso` para responder `ANTERIOR AO PISO` **pela ancestralidade** quando a mensagem cala (T1/C1 d); sem o `piso`, o leitor não tem contra o que medir. Caso da sonda que consome o campo: **F4** (`anterior_ao_piso: true` ⇒ `ANTERIOR AO PISO`, 0 problemas). Árvore real (`fecho.json → _meta.prova_de_carga_leitura_fase4.ancestralidade`): os 10 `red.commit` são ancestrais de `origin/develop` **e** do piso — o segundo teste é o que separa "mesclada" de "anterior" |
| **ET2** | T030: `ler_merges(piso)` por `git log --format=%H%x00%cI%x00%s --merges --first-parent …` (idem `plan.md` §Stage, "um `git log --merges --first-parent`"; e **a `spec.md` §Superfície 1 "Entrada"** carrega o mesmo `--merges` — ver nota abaixo) | `ler_merges(piso)` percorre a cadeia first-parent **inteira** com `%P` (`--format=%H%x00%P%x00%cI%x00%s`, sem `--merges`), localiza o piso e anota cada merge (`≥ 2 pais`) com a posição; `piso_na_cadeia` sai da cadeia inteira — docstring do gate; `fecho.json → piso.descricao` e `_meta.prova_de_carga_leitura_fase4.metodo`; `red-016.md` §7; `fecho.py:459-503` | Medido por git em 2026-09-04: o piso zero da prova de carga (`e5ccd429`, T060 c / `D016-M19`) é a **raiz** (0 pais), posição 67 de 67 na first-parent de `origin/develop` (39 merges). `git log --first-parent --merges … \| grep -c ^e5ccd429` ⇒ **0**; sem `--merges` ⇒ **1**. Com `--merges` o leitor devolveria `piso_na_cadeia: false`, o julgador abortaria em `piso-invalido` (forma de F21) e a prova de carga de C2(a) — 6 merges / 5 branches — seria **inexecutável**; um piso legítimo não-merge viraria falso positivo. O julgamento dos merges é idêntico nas duas formas: só `≥ 2 pais` vira sujeito (`merges_ate_piso` = 39 na árvore real, igual ao censo por `--merges`) |
| **ET3** | T030 / `plan.md` §Contratos: retorno `{sujeitos, contagens, problemas}` — sem `globais`; o docstring do gate (antes deste commit) enumerava em `globais` só `piso-invalido` e `origin-develop-ausente` | `globais` tem **duas classes**: impeditivos (`piso-invalido` \| `origin-develop-ausente`, no máximo um por execução, por precedência — `fecho.py` docstring, decisões 1–2) e **não impeditivo** `exclusao-malformada` (uma entrada por exclusão inválida; os sujeitos seguem julgados). Docstring do gate emendado neste commit (shape do retorno e alínea "sob um global") | Bateria em worktree efêmera sobre `83a88e2` (2026-09-04), exclusão da 009 com `fonte: ""` (forma de `D016-M27`): **instrumento real** ⇒ `[FAIL] exclusão R13 de 009-leitura-do-relatorio NÃO exclui (sem \`fonte\`)` + `009: MESCLADA SEM FECHO` + `2 problema(s)`, exit 1. **Mutante em cópia** (malformada só em `problemas`; `ast.parse` ok) ⇒ `2 problema(s)`, exit 1, **e a linha da malformada não é impressa** (`--json`: `globais: []`, `problemas` com 2) — problema contado e não dito, julgador mudo (C7). A sonda **não** distingue as duas formas (33 · 0 sob o mutante: F23 pina veredito, código e total, não o lugar); quem mata é o gate nu — `D016-M27` (T060). A terceira forma, abortar sob malformada, morre em F23 (o sujeito sai `MESCLADA SEM FECHO`/`artefato-ausente`, não `NÃO DETERMINÁVEL`) |
| **ET4** | T032(v): `grep -nE "urllib\|http\.\|socket\|ssl" …` ⇒ zero — a varredura, como estava, **acusava o próprio gate** (`check_fecho.py:23`, a lista nominal em prosa): R10 §10, a família que a 013 pagou (`IC-1`) | (a) Os dois arquivos varridos **não nomeiam** as bibliotecas — a lista nominal vive **só na definição da varredura** (célula de T032), fora do conjunto varrido: auto-exclusão nominal na forma que não abre furo (nada a excluir). `check_fecho.py:23` reformulado neste commit; `fecho.py:13-15` já não as nomeava. (b) T032(v) passa a `grep -nwE "urllib\|http\|socket\|ssl"` — **palavra inteira, arquivo inteiro** | Bateria adversarial de 16 arquivos (GNU grep 3.0, 2026-09-04), três formas — **P** prescrita, **B** "só linhas de import" (`^\s*(import\|from)\s…`), **W** palavra inteira (`-w`). **B é afrouxamento** (R10 §1): deixa vivos `x = 1; import urllib` (A03), `__import__("socket")` (A04), `importlib.import_module("http.client")` (A05), `exec("import ssl")` (A06) e `nome = "urllib"; __import__(nome)` (A13) — 5 evasões que P mata. **P tem dois cegos**: `from http import client` (A08) e `import http` (A09) escapam de `http\.`, e a propriedade ratificada (`plan.md` §Camada; T020 "zero `urllib`/`http`/`socket`/`ssl`") nomeia a biblioteca, não o ponto. **W** mata tudo que P mata **mais** A08/A09, e deixa de acusar substring (`sslcontext` A14, "sockets" A15). Sobre os dois arquivos: P e W davam **1 · 1** (ambos `check_fecho.py:23`) e passam a **0 · 0**. O que **nenhuma** forma pega, declarado: nome montado em tempo de execução (`"ht"+"tp"`) — scanner de padrão não é sandbox; a garantia estrutural é o stage não ter rede (plan §Camada), o scanner é a cobrança barata |

**O que NÃO muda**: ids, donos, tipagem, waves, os "26" históricos (desvio já
aceito no planning-state). **A `spec.md` não é emendada aqui**: onde ela carrega
o mesmo `--merges` (§Superfície 1, "Entrada") a classe é `spec-errada`
(skill `spec-validate` §3) e a correção — uma E3 com o texto de ET2 — exige
aprovação do usuário; levada por `DEPENDÊNCIAS`.
